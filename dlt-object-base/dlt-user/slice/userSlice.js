import { createObjectSlice } from '@dlt-object-base/dlt-base/utils/createSlice';
import StoreConfig from '@dlt-object-base/storeConfig';

const userSlice = createObjectSlice(StoreConfig.user);

export default userSlice;
